# Environment secrets — never commit real credentials
.env
.env.local
.env.*.local

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Dependencies (if a build step is added later)
node_modules/
dist/
.cache/
